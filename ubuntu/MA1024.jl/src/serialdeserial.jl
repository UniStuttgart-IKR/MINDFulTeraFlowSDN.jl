function routerviewtojson(rv::RouterView)
    # Convert RouterView to JSON
   # Take the fields and put them into some reprentation for config_rules
   # configrulesfrom(rv::RouterView, router, portnumber
end

function configrulesfrom(rv::RouterView, router, portnumber) end